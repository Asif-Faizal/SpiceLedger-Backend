# =============================================================================
# SpiceLedger — Local development (Homebrew MySQL on localhost)
# Copy to .env:  cp .env.local.example .env
# =============================================================================

APP_ENV=development

# Database (local MySQL)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=1234
DB_NAME=spice_ledger

# Auth
JWT_SECRET=supersecretjwtkey123!
ACCESS_TOKEN_DURATION=30m
REFRESH_TOKEN_DURATION=168h
BASIC_AUTH_USER=admin
BASIC_AUTH_PASS=secret123

# Service ports
CONTROL_GRPC_PORT=50051
MARKET_GRPC_PORT=50052
REST_PORT=8082
GRAPHQL_PORT=8081
PROXY_PORT=8080

# Inter-service URLs (localhost for native `go run`; leave blank to use port defaults)
ACCOUNT_GRPC_URL=localhost:50051
MARKET_GRPC_URL=localhost:50052
ACCOUNT_SERVICE_URL=http://localhost:8082
GRAPHQL_GATEWAY_URL=http://localhost:8081

LOG_LEVEL=debug
